Pre-recorded Nepali audio goes here, named after the romanised line with
underscores, e.g. ma_yahaa_chu.wav

Record on a phone, export as wav. The phrase loop prints to stdout and plays
any file it finds, so it runs fine with this folder empty.
